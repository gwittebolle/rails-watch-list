<div class="card mb-3 col-3 p-0 m-0 d-flex flex-column justify-content-between">
  <%= list.photo.attached? ? cl_image_tag(list.photo.key, :height=>200, :gravity=>"auto", :crop=>"fill") :  image_tag('pellicule.jpg', height: 200) %>
  <div class="card-body d-flex flex-column justify-content-between">
    <h4 class="card-title"><strong><%= list.name %></strong></h4>
    <p class="card-text"><i>Nombre de films : <%= list.bookmarks.count %></i></p>
    <div class="d-flex justify-content-between mt-auto border-top pt-2">
      <%= link_to 'Voir la sélection', list_path(list) %>
      <%= link_to 'Supprimer', list_path(list), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
    </div>
  </div>
</div>
